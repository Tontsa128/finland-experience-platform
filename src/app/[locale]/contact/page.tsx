"use client";
import { FormEvent,useState } from "react"; import { Mail,Phone,MapPin,Loader2 } from "lucide-react"; import { useTranslations } from "next-intl";
export default function ContactPage(){
  const t=useTranslations("contact"); const [sent,setSent]=useState(false); const [loading,setLoading]=useState(false); const [error,setError]=useState("");
  async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const form=new FormData(e.currentTarget);try{const res=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:form.get("name"),email:form.get("email"),message:form.get("message")})});const data=await res.json();if(data.fallback&&data.mailto){window.location.href=data.mailto;setSent(true);}else if(!res.ok)throw new Error();else setSent(true);}catch{setError(t("error"));}finally{setLoading(false);}}
  return <div className="container-narrow py-16"><div className="max-w-2xl"><h1 className="section-title">{t("title")}</h1><p className="section-subtitle mt-3">{t("subtitle")}</p></div><div className="mt-10 grid gap-10 lg:grid-cols-2"><div className="space-y-5 text-slate-600"><a href="mailto:hello@nordicescape.fi" className="flex gap-3 hover:text-brand-700"><Mail/>hello@nordicescape.fi</a><p className="flex gap-3"><Phone/>+358 40 000 0000</p><p className="flex gap-3"><MapPin/>Finland</p><div className="rounded-2xl bg-brand-50 p-5 text-sm leading-relaxed text-brand-900">{t("providerNote")}</div></div><form onSubmit={submit} className="space-y-4 rounded-2xl border bg-white p-6 shadow-soft"><input name="name" required placeholder={t("name")} className="w-full rounded-xl border px-4 py-3"/><input name="email" required type="email" placeholder={t("email")} className="w-full rounded-xl border px-4 py-3"/><textarea name="message" required placeholder={t("message")} rows={6} className="w-full rounded-xl border px-4 py-3"/>{error&&<p className="text-sm text-red-600">{error}</p>}<button disabled={loading||sent} className="btn-primary flex w-full items-center justify-center gap-2">{loading&&<Loader2 className="h-4 w-4 animate-spin"/>}{sent?t("success"):t("send")}</button></form></div></div>
}