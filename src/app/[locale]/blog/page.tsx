import { blogPosts,getLocalized } from "@/lib/data"; import type { Locale } from "@/types";
export default function BlogPage({params}:{params:{locale:string}}){const locale=params.locale as Locale;return <div className="container-narrow py-16"><h1 className="section-title">Blog</h1><p className="section-subtitle mt-3 mb-10">Travel inspiration and practical tips for Finland.</p><div className="grid gap-6 md:grid-cols-2">{blogPosts.map(p=><article key={p.id} className="rounded-2xl border bg-white p-6 shadow-soft"><div className="text-xs text-brand-600">{p.publishedAt}</div><h2 className="mt-2 font-display text-2xl font-semibold text-brand-900">{getLocalized(p.title,locale)}</h2><p className="mt-3 text-slate-600">{getLocalized(p.excerpt,locale)}</p><div className="mt-4 flex flex-wrap gap-2">{p.tags.map(tag=><span key={tag} className="rounded-full bg-slate-100 px-3 py-1 text-xs">{tag}</span>)}</div></article>)}</div></div>}
