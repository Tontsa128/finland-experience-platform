import Link from "next/link"; import { experiences,getLocalized } from "@/lib/data"; import type { Locale } from "@/types";
export default function ExperiencesPage({params}:{params:{locale:string}}){const locale=params.locale as Locale;return <div className="container-narrow py-16"><h1 className="section-title">Experiences</h1><p className="section-subtitle mt-3 mb-10">Authentic Finnish adventures, from auroras to sauna.</p><div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{experiences.map(e=><Link key={e.id} href={`/${locale}/experiences/${e.slug}`} className="rounded-2xl border bg-white p-6 shadow-soft hover:shadow-card"><h2 className="font-display text-xl font-semibold text-brand-900">{getLocalized(e.name,locale)}</h2><p className="mt-2 text-sm text-slate-600">{getLocalized(e.shortDescription,locale)}</p><div className="mt-4 flex justify-between font-semibold text-brand-700"><span>€{e.price}</span><span>{e.duration}</span></div></Link>)}</div></div>}
