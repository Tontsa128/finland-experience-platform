import Link from "next/link"; import { experiences,getLocalized } from "@/lib/data"; import type { Locale } from "@/types";
export default function ExperiencesPage({params}:{params:{locale:string}}){const locale=params.locale as Locale;return <div className="container-narrow py-16"><p className="text-sm font-semibold uppercase tracking-[.18em] text-brand-600">Summer • Southern Finland</p><h1 className="mt-2 section-title">{locale==="fi"?"Tekemistä ja elämyksiä":locale==="es"?"Qué hacer y experiencias":"Things to do & experiences"}</h1><p className="section-subtitle mt-3 mb-10">{locale==="fi"?"Poimintoja oikeista paikallisista palveluista ja kesätekemisestä. Varaa aina suoraan palveluntarjoajalta.":locale==="es"?"Selección de actividades y servicios locales. Reserva directamente con el proveedor.":"A selection of real local activities and services. Book directly with the provider."}</p><div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{experiences.map(e=><Link key={e.id} href={"/"+locale+"/experiences/"+e.slug} className="rounded-2xl border bg-white p-6 shadow-soft hover:shadow-card"><h2 className="font-display text-xl font-semibold text-brand-900">{getLocalized(e.name,locale)}</h2><p className="mt-2 text-sm text-slate-600">{getLocalized(e.shortDescription,locale)}</p><div className="mt-4 flex justify-between font-semibold text-brand-700"><span>{e.price>0?"€"+e.price+"+":locale==="fi"?"Kysy hinta":locale==="es"?"Consultar precio":"Ask provider"}</span><span>{e.duration}</span></div></Link>)}</div></div>}