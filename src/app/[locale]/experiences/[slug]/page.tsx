import { notFound } from "next/navigation"; import Image from "next/image"; import { experiences,getLocalized } from "@/lib/data"; import { BookingWidget } from "@/components/booking/BookingWidget"; import type { Locale } from "@/types";
export default function ExperienceDetail({params}:{params:{locale:string;slug:string}}){const locale=params.locale as Locale;const e=experiences.find(x=>x.slug===params.slug);if(!e)notFound();return <div className="container-narrow py-12"><div className="grid gap-10 lg:grid-cols-[1fr_380px]"><div><div className="relative aspect-video overflow-hidden rounded-3xl"><Image src={e.images[0]} alt={getLocalized(e.name,locale)} fill sizes="(max-width:1024px) 100vw,70vw" className="object-cover"/></div><div className="mt-8 flex gap-3 text-sm text-brand-700"><span>{e.region}</span><span>•</span><span>{e.duration}</span></div><h1 className="mt-3 section-title">{getLocalized(e.name,locale)}</h1><p className="mt-4 text-lg leading-relaxed text-slate-600">{getLocalized(e.description,locale)}</p></div><BookingWidget itemType="experience" itemId={e.id} price={e.price} priceLabel="per person" locale={locale} maxGuests={e.maxParticipants||12} duration={e.duration}/></div></div>}
