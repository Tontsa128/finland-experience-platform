import Link from "next/link"; import { cabins,getLocalized } from "@/lib/data"; import type { Locale } from "@/types";
export default function AccommodationsPage({params}:{params:{locale:string}}){const locale=params.locale as Locale;return <div className="container-narrow py-16"><h1 className="section-title">Accommodations</h1><p className="section-subtitle mt-3 mb-10">Stay in glass cabins, igloos, villas and design hotels.</p><div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{cabins.map(c=><Link key={c.id} href={`/${locale}/accommodations/${c.slug}`} className="rounded-2xl border bg-white p-6 shadow-soft hover:shadow-card"><h2 className="font-display text-xl font-semibold text-brand-900">{getLocalized(c.name,locale)}</h2><p className="mt-2 text-sm text-slate-600">{c.location}</p><p className="mt-4 font-semibold text-brand-700">€{c.pricePerNight} / night</p></Link>)}</div></div>}
