import { notFound } from "next/navigation"; import Image from "next/image"; import { cabins,getLocalized } from "@/lib/data"; import { BookingWidget } from "@/components/booking/BookingWidget"; import type { Locale } from "@/types";
export default function CabinDetail({params}:{params:{locale:string;slug:string}}){const locale=params.locale as Locale;const c=cabins.find(x=>x.slug===params.slug);if(!c)notFound();return <div className="container-narrow py-12"><div className="grid gap-10 lg:grid-cols-[1fr_380px]"><div><div className="relative aspect-video overflow-hidden rounded-3xl"><Image src={c.images[0]} alt={getLocalized(c.name,locale)} fill sizes="(max-width:1024px) 100vw,70vw" className="object-cover"/></div><h1 className="mt-8 section-title">{getLocalized(c.name,locale)}</h1><p className="mt-4 text-lg leading-relaxed text-slate-600">{getLocalized(c.description,locale)}</p><div className="mt-6 grid grid-cols-2 gap-2 sm:grid-cols-3">{c.features.map(f=><span key={f} className="rounded-lg bg-brand-50 px-3 py-2 text-sm text-brand-800">{f}</span>)}</div></div><BookingWidget itemType="cabin" itemId={c.id} price={c.pricePerNight} priceLabel="per night" locale={locale} maxGuests={c.maxGuests}/></div></div>}
