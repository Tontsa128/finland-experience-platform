import Link from "next/link"; import { destinations } from "@/lib/data"; import { getLocalized } from "@/lib/data"; import type { Locale } from "@/types";
export default async function DestinationsPage({params}:{params:{locale:string}}){const locale=params.locale as Locale;return <div className="container-narrow py-16"><h1 className="section-title">Destinations</h1><p className="section-subtitle mt-3 mb-10">Discover Finland from Lapland to the southern coast.</p><div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{destinations.map(d=><Link key={d.id} href={`/${locale}/destinations/${d.slug}`} className="rounded-2xl border bg-white p-6 shadow-soft hover:shadow-card"><h2 className="font-display text-xl font-semibold text-brand-900">{getLocalized(d.name,locale)}</h2><p className="mt-2 text-sm text-slate-600">{getLocalized(d.shortDescription,locale)}</p><p className="mt-4 font-semibold text-brand-700">From €{d.priceFrom}</p></Link>)}</div></div>}
