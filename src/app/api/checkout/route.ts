import { NextRequest,NextResponse } from "next/server";
export async function POST(req:NextRequest){try{const body=await req.json();const {itemType,itemId,price,guests,dateFrom,dateTo,locale}=body;if(!itemType||!itemId||typeof price!=="number"||price<=0)return NextResponse.json({error:"Missing or invalid required fields"},{status:400});if(!Number.isFinite(price)||price>100000)return NextResponse.json({error:"Invalid price"},{status:400});return NextResponse.json({message:"Stripe integration ready. Add STRIPE_SECRET_KEY to enable real payments.",demo:true,received:{itemType,itemId,price,guests,dateFrom,dateTo,locale}})}catch(error){console.error("Checkout error:",error);return NextResponse.json({error:"Internal server error"},{status:500})}}
