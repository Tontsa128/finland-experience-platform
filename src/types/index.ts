export type Locale = "fi" | "es" | "en";

export interface Destination { id:string; slug:string; name:Record<Locale,string>; description:Record<Locale,string>; shortDescription:Record<Locale,string>; region:string; images:string[]; priceFrom:number; featured:boolean; coordinates?:{lat:number;lng:number}; tags:string[]; accommodationIds?:string[]; activities?:string[]; }
export interface Cabin { id:string; slug:string; name:Record<Locale,string>; description:Record<Locale,string>; location:string; region:string; pricePerNight:number; images:string[]; features:string[]; maxGuests:number; bedrooms:number; coordinates?:{lat:number;lng:number}; type:"cabin"|"igloo"|"hotel"|"villa"|"glamping"; bookingUrl?:string; provider?:string; priceNote?:Record<Locale,string>; }
export interface Experience { id:string; slug:string; name:Record<Locale,string>; description:Record<Locale,string>; shortDescription:Record<Locale,string>; price:number; duration:string; images:string[]; category:"northern-lights"|"husky"|"sauna"|"cruise"|"hiking"|"food"|"culture"; region:string; maxParticipants?:number; }
export interface Booking { id:string; userId:string; itemType:"destination"|"cabin"|"experience"; itemId:string; dateFrom:string; dateTo?:string; guests:number; price:number; status:"pending"|"confirmed"|"cancelled"|"completed"; createdAt:string; }
export interface User { id:string; email:string; role:"user"|"admin"; name?:string; preferredLocale?:Locale; }
export interface BlogPost { id:string; slug:string; title:Record<Locale,string>; excerpt:Record<Locale,string>; content:Record<Locale,string>; image:string; author:string; publishedAt:string; tags:string[]; }
